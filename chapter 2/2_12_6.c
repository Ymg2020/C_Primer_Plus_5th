#include <stdio.h>
int main(void)
{

int tose = 10;
printf("tose is %d\n",tose);
int tose2 = tose*2;
printf("The twice of tose is %d\n",tose2);
int tose3 = tose*tose;
printf("The square of tose is %d\n",tose3);

}
